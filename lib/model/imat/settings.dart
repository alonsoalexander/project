class Settings {
  // ── Grupp-ID ────────────────────────────────────────────────────────────────
  // Ändra detta till ert grupps ID för att använda er egna data på servern.
  static const int groupId = 14;
}
